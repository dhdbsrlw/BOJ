#include <stdio.h>
#pragma warning(disable: 4996)	

int main(void)
{
	int A, B;
	int i, T;

	scanf("%d", &T);

	for (i = 1; i <= T; i++) {
		scanf("%d %d", &A, &B);
		printf("%d\n", A + B);
	}

	return 0;

}
