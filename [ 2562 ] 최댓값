#include <stdio.h>
#pragma warning(disable: 4996)	

int main(void)
{
	int num, i, maxi;
	int max = 0;

	for (i = 1, maxi = 1; i <= 9; i++)
	{
		scanf("%d", &num);

		if (num > max)
		{
			max = num;
			maxi = i;
		}
	}

	printf("%d\n%d", max, maxi);

	return 0;
}
