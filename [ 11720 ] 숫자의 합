#include <stdio.h>
#pragma warning(disable: 4996)


int main(void) {

	int sum = 0;
	int n;
	char num[100] = {0, };
	
	scanf("%d %s", &n, num);

	for (int i = 0; i < n; i++) {

		sum += (int)num[i] - 48;

	}

	printf("%d", sum);

	return 0;
}
