import sys
input = sys.stdin.readline

N = int(input())

ans = []
for i in range(N):
    [x, y] = map(int, input().split())
    ans.append([x,y])

ans = sorted(ans)

for i in range(N):
    print(ans[i][0], ans[i][1])
